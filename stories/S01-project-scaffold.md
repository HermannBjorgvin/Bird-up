# S01 — Project scaffold: deployable Worker skeleton

**Epic:** scaffolding · **Depends on:** nothing · **Spec refs:** [08-tech-stack](../specs/08-tech-stack.md), [01-architecture](../specs/01-architecture.md)

> As a developer, I want a typed, tested, manually-deployable Worker skeleton, so that every later story lands on rails instead of yak-shaving.

## Description

The repo becomes a working TypeScript Cloudflare Worker project with the exact version pins, config shapes and directory layout from spec 08: `wrangler.jsonc` (assets binding, KV, both cron expressions, `BASE_URL` var), Hono entry point with `/api/health` and an empty cron dispatcher, the two-project vitest setup (unit + workers pool, **post-0.13 API**), generated `Env` types, and the secret/`.dev.vars` convention. Ends with the first real manual deploy.

## Acceptance criteria

- [x] `package.json` pins the spec-08 versions; `npm install` is clean on Node 22 LTS; `npm run check` (tsc + both vitest projects) and `npm test` scripts exist.
- [x] `wrangler.jsonc` matches the spec-08 shape: `run_worker_first: ["/api/*", "/mcp", "/mcp/*"]`, `ASSETS` + `KV` bindings, both cron triggers, `BASE_URL` var, `nodejs_compat`; `worker-configuration.d.ts` generated by `wrangler types` and committed.
- [x] `src/` skeleton follows the spec-01 tree (empty `core/ports/adapters/delivery/jobs` directories may hold only placeholder modules); `src/index.ts` exports `fetch` (Hono) and `scheduled` (dispatcher keyed by cron expression) as `satisfies ExportedHandler<Env>`.
- [x] One unit-project test passes (trivial pure function) and one worker-project test passes: `GET /api/health` via `exports.default.fetch` returns `{ ok: true }`, and a KV put/get round-trips via `env.KV` — proving the pool, bindings and config path all work.
- [x] A worker-project test invokes the scheduled handler via `createScheduledController` without error (dispatcher runs, does nothing yet).
- [x] `.dev.vars` is git-ignored and documented; no secret value appears anywhere in the repo.
- [x] **First manual deploy performed**: `wrangler deploy` succeeds; `https://tjaldur.9z.is/api/health` returns 200 `{ ok: true }` (custom domain on the owner's `9z.is` zone — decided 2026-06-12, supersedes workers.dev); the URL is recorded in this story's notes and set as `BASE_URL`.

## Demo

Clone → `npm install` → `npm run check` green → `npx wrangler deploy` → `/api/health` live at tjaldur.9z.is.

## Notes

Completed 2026-06-12.

- **Deployed URL**: https://tjaldur.9z.is (Workers custom domain route, `BASE_URL` set to match; no workers.dev URL — routes disable it). First version ID `8144f557-172c-4838-b3f8-056c2f648039`.
- **KV namespace**: `09be49343b8f4182af17c775bcda9acd` (binding `KV`).
- **Pin deviations from the original spec draft** (spec 08 updated to match): the owner's npm quarantine (`min-release-age=7` in `~/.npmrc`) blocks packages younger than a week, so wrangler is 4.98.0 (which is also exactly what pool-workers 0.16.13 ships — better miniflare match than 4.100.0), vitest 4.1.8, typescript 5.9.3. `compatibility_date` is 2026-06-01 because the pool's workerd is 2026-06-03.
- **Pool API corrections discovered here** (recorded in spec 08): `exports.default.fetch(request)` only — no env/ctx args; `scheduled` must be called on a direct `src/index` import because `ScheduledController` doesn't serialize through the loopback binding.
- `dist/client/` (git-ignored) must exist before `wrangler deploy`; tests don't need it. A placeholder `index.html` is there until S02's `vite build` takes over.
