{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty'); case \"$f\" in *.ts|*.tsx|*.js|*.jsx) out=$(npx eslint --no-warn-ignored --max-warnings 0 \"$f\" 2>&1) || { echo \"$out\" >&2; exit 2; };; esac",
            "timeout": 60,
            "statusMessage": "Linting written file"
          }
        ]
      }
    ]
  }
}
